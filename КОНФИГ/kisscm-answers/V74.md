## Решение вариант 74 
 1. 
```bash
#!/bin/bash
#launch: ./[name_of_script].sh [directory]

directory=$1
ls $directory | grep -E '^[a-z]+.txt'
``` 
2.
 ```bash
git branch barterer
git branch slick
git checkout -b enclosure
git commit zodiac
git checkout master
git commit strudel
git merge enclosure
git checkout enclosure
git merge master
git checkout master
git commit unstable
git checkout enclosure
git merge master
git checkout master
git commit never
git branch revival
git commit sierra
```
