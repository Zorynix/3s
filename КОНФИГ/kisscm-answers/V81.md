## Решение вариант 81 
 1. 
```bash
git commit -m dreamily
git checkout -b unroll
git commit -m thermal
git checkout -b graph
git commit -m basil
git checkout master
git commit -m decimeter
git merge graph
git checkout -b sensually
git commit -m lego
```
2
 ```bash
(
  (happened
    (failed
      (start true)
      (kitchen 204601934.67446375)
      (found true)
      (paragraph true)
      (consider "will")
      (special -1393297861)
    )
    (valuable "heavy")
    (too false)
    (whose false)
    (sum -1291549897)
    (hurry -1038277552.9540367)
  )
  (roll true)
  (where "acres")
  (month "lamp")
  (football "root")
  (earn "silver")
)

``` 