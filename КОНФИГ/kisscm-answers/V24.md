## Решение вариант 24 
 1. 
 ```bash
(
  (happened
    (failed
      (start true)
      (kitchen 204601934.67446375)
      (found true)
      (paragraph true)
      (consider "will")
      (special -1393297861)
    )
    (valuable "heavy")
    (too false)
    (whose false)
    (sum -1291549897)
    (hurry -1038277552.9540367)
  )
  (roll true)
  (where "acres")
  (month "lamp")
  (football "root")
  (earn "silver")
)

``` 
 2.
 ```bash
git branch barterer
git branch slick
git checkout -b enclosure
git commit zodiac
git checkout master
git commit strudel
git merge enclosure
git checkout enclosure
git merge master
git checkout master
git commit unstable
git checkout enclosure
git merge master
git checkout master
git commit never
git branch revival
git commit sierra
```
